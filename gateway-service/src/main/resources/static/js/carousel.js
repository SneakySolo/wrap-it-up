const cards=[...document.querySelectorAll('.wrap-card')],prev=document.querySelector('#prev'),next=document.querySelector('#next'),current=document.querySelector('#current'),bar=document.querySelector('#progress-bar');let index=0;function show(i){index=i;cards.forEach((card,n)=>card.classList.toggle('active',n===i));current.textContent=i+1;prev.disabled=i===0;next.disabled=i===cards.length-1;bar.style.width=`${((i+1)/cards.length)*100}%`}prev.onclick=()=>index>0&&show(index-1);next.onclick=()=>index<cards.length-1&&show(index+1);document.addEventListener('keydown',event=>{if(event.key==='ArrowLeft'&&!prev.disabled)show(index-1);if(event.key==='ArrowRight'&&!next.disabled)show(index+1)});show(0);
